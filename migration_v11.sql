-- ============================================================
-- ОБНОВЛЕНИЕ v11 — выполнить в Supabase ОДИН РАЗ до деплоя кода
-- Supabase → SQL Editor → New query → вставить → Run
--
-- Добавляет в records поле prev_seller — имя предыдущего селлера.
-- Заполняется при нажатии «Долго!» в Модерации: при создании клона
-- в Б/У туда пишется селлер исходной записи, чтобы при повторной
-- сортировке было видно, кому уже отдавали этот домен.
-- ============================================================

ALTER TABLE records ADD COLUMN IF NOT EXISTS prev_seller TEXT DEFAULT '';
